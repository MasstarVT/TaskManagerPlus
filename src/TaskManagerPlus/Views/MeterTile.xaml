<UserControl x:Class="TaskManagerPlus.Views.MeterTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="Root">
    <!-- Small dashboard widget: colored dot + title, a big value, and a colored bar with a
         subtext underneath - the denser "gauge" tile style used on the Summary page. -->
    <Border Style="{StaticResource CardStyle}" Margin="0">
        <StackPanel>
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="9" Height="9" CornerRadius="4.5" Margin="0,0,8,0"
                            Background="{Binding AccentBrush, ElementName=Root}" />
                    <TextBlock Text="{Binding Title, ElementName=Root}" Style="{StaticResource CardTitle}" Margin="0" />
                </StackPanel>
                <TextBlock Grid.Column="1" Text="{Binding ValueText, ElementName=Root}"
                           FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" />
            </Grid>
            <ProgressBar Value="{Binding Percent, ElementName=Root}" Maximum="100" Height="8"
                         Foreground="{Binding AccentBrush, ElementName=Root}" />
            <TextBlock Text="{Binding SubText, ElementName=Root}" Style="{StaticResource SubStat}" Margin="0,8,0,0" />
        </StackPanel>
    </Border>
</UserControl>
