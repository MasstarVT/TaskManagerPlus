<UserControl x:Class="TaskManagerPlus.Views.PerformanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:TaskManagerPlus.ViewModels"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             d:DataContext="{d:DesignInstance Type=vm:PerformanceViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="ColorDot" TargetType="Border">
            <Setter Property="Width" Value="9" />
            <Setter Property="Height" Value="9" />
            <Setter Property="CornerRadius" Value="4.5" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="TitleRow" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal" />
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
        <StackPanel>

            <!-- Summary tiles -->
            <UniformGrid Columns="4" Margin="0,0,0,0">
                <Border Style="{StaticResource CardStyle}" Margin="0,0,8,16">
                    <StackPanel>
                        <StackPanel Style="{StaticResource TitleRow}" Margin="0,0,0,4">
                            <Border Style="{StaticResource ColorDot}" Background="{Binding CpuColor, Converter={StaticResource ColorToBrushConverter}}" />
                            <TextBlock Text="CPU" Style="{StaticResource CardTitle}" Margin="0" />
                        </StackPanel>
                        <TextBlock Text="{Binding CpuCurrentPercent, StringFormat={}{0:0.0}%}" Style="{StaticResource BigStat}" />
                        <TextBlock Text="{Binding CpuCurrentClockGhz, StringFormat={}{0:0.00} GHz}" Style="{StaticResource SubStat}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}" Margin="4,0,4,16">
                    <StackPanel>
                        <StackPanel Style="{StaticResource TitleRow}" Margin="0,0,0,4">
                            <Border Style="{StaticResource ColorDot}" Background="{Binding RamColor, Converter={StaticResource ColorToBrushConverter}}" />
                            <TextBlock Text="Memory" Style="{StaticResource CardTitle}" Margin="0" />
                        </StackPanel>
                        <TextBlock Text="{Binding RamPercent, StringFormat={}{0:0.0}%}" Style="{StaticResource BigStat}" />
                        <TextBlock Style="{StaticResource SubStat}">
                            <Run Text="{Binding RamUsedGb, StringFormat={}{0:0.0}, Mode=OneWay}" /><Run Text=" / " /><Run Text="{Binding RamTotalGb, StringFormat={}{0:0.0}, Mode=OneWay}" /><Run Text=" GB" />
                        </TextBlock>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}" Margin="4,0,4,16">
                    <StackPanel>
                        <StackPanel Style="{StaticResource TitleRow}" Margin="0,0,0,4">
                            <Border Style="{StaticResource ColorDot}" Background="{Binding DiskColor, Converter={StaticResource ColorToBrushConverter}}" />
                            <TextBlock Text="Disk" Style="{StaticResource CardTitle}" Margin="0" />
                        </StackPanel>
                        <TextBlock Text="{Binding DiskPercent, StringFormat={}{0:0.0}%}" Style="{StaticResource BigStat}" />
                        <TextBlock Text="Active time" Style="{StaticResource SubStat}" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}" Margin="8,0,0,16">
                    <StackPanel>
                        <StackPanel Style="{StaticResource TitleRow}" Margin="0,0,0,4">
                            <Border Style="{StaticResource ColorDot}" Background="{Binding NetworkColor, Converter={StaticResource ColorToBrushConverter}}" />
                            <TextBlock Text="Network" Style="{StaticResource CardTitle}" Margin="0" />
                        </StackPanel>
                        <TextBlock Text="{Binding NetworkReceiveBps, Converter={StaticResource BytesToReadableConverter}, ConverterParameter=rate}" Style="{StaticResource BigStat}" />
                        <TextBlock Text="{Binding NetworkSendBps, Converter={StaticResource BytesToReadableConverter}, ConverterParameter=rate, StringFormat=Up: {0}}" Style="{StaticResource SubStat}" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- CPU chart + per-core grid -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Style="{StaticResource TitleRow}" Margin="0">
                            <Border Style="{StaticResource ColorDot}" Background="{Binding CpuColor, Converter={StaticResource ColorToBrushConverter}}" />
                            <TextBlock Text="{Binding CpuName}" Style="{StaticResource CardTitle}" Margin="0" />
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{Binding CpuSpecs}" Style="{StaticResource SubStat}" VerticalAlignment="Center" />
                    </Grid>
                    <lvc:CartesianChart Series="{Binding CpuSeries}" XAxes="{Binding HiddenXAxes}" YAxes="{Binding PercentYAxes}"
                                         Height="180" TooltipPosition="Hidden" LegendPosition="Hidden" />
                    <ItemsControl ItemsSource="{Binding Cores}" Margin="0,14,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="70" Height="54" Margin="0,0,8,8" Background="{StaticResource BgElevatedBrush}"
                                        BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="6" Padding="9,7">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Label}" Style="{StaticResource SubStat}" FontSize="10.5" />
                                        <TextBlock Text="{Binding Percent, StringFormat={}{0:0}%}" FontSize="16" FontWeight="SemiBold"
                                                   Foreground="{Binding Percent, Converter={StaticResource PercentToBrushConverter}}" Margin="0,2,0,0" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Memory chart -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <StackPanel Style="{StaticResource TitleRow}">
                        <Border Style="{StaticResource ColorDot}" Background="{Binding RamColor, Converter={StaticResource ColorToBrushConverter}}" />
                        <TextBlock Text="Memory" Style="{StaticResource CardTitle}" Margin="0" />
                    </StackPanel>
                    <lvc:CartesianChart Series="{Binding RamSeries}" XAxes="{Binding HiddenXAxes}" YAxes="{Binding PercentYAxes}"
                                         Height="140" TooltipPosition="Hidden" LegendPosition="Hidden" />
                </StackPanel>
            </Border>

            <!-- Disk chart -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <StackPanel Style="{StaticResource TitleRow}">
                        <Border Style="{StaticResource ColorDot}" Background="{Binding DiskColor, Converter={StaticResource ColorToBrushConverter}}" />
                        <TextBlock Text="Disk" Style="{StaticResource CardTitle}" Margin="0" />
                    </StackPanel>
                    <lvc:CartesianChart Series="{Binding DiskSeries}" XAxes="{Binding HiddenXAxes}" YAxes="{Binding PercentYAxes}"
                                         Height="140" TooltipPosition="Hidden" LegendPosition="Hidden" />
                </StackPanel>
            </Border>

            <!-- Network chart -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <StackPanel Style="{StaticResource TitleRow}">
                        <Border Style="{StaticResource ColorDot}" Background="{Binding NetworkColor, Converter={StaticResource ColorToBrushConverter}}" />
                        <TextBlock Text="Network" Style="{StaticResource CardTitle}" Margin="0" />
                    </StackPanel>
                    <lvc:CartesianChart Series="{Binding NetworkSeries}" XAxes="{Binding HiddenXAxes}" YAxes="{Binding NetworkYAxes}"
                                         Height="140" TooltipPosition="Top" LegendPosition="Bottom" />
                </StackPanel>
            </Border>

            <!-- System summary -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="System" Style="{StaticResource CardTitle}" />
                    <UniformGrid Columns="4">
                        <StackPanel Margin="0,0,8,0">
                            <TextBlock Text="Processes" Style="{StaticResource SubStat}" />
                            <TextBlock Text="{Binding ProcessCount}" FontSize="18" />
                        </StackPanel>
                        <StackPanel Margin="8,0,8,0">
                            <TextBlock Text="Threads" Style="{StaticResource SubStat}" />
                            <TextBlock Text="{Binding ThreadCount}" FontSize="18" />
                        </StackPanel>
                        <StackPanel Margin="8,0,8,0">
                            <TextBlock Text="Handles" Style="{StaticResource SubStat}" />
                            <TextBlock Text="{Binding HandleCount}" FontSize="18" />
                        </StackPanel>
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="Uptime" Style="{StaticResource SubStat}" />
                            <TextBlock Text="{Binding Uptime}" FontSize="18" />
                        </StackPanel>
                    </UniformGrid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
