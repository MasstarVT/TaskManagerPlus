<UserControl x:Class="TaskManagerPlus.Views.VfdMeter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="Root">
    <!-- TMOG-style "VFD meter": a dark inset bezel, a big monospace readout with an optional
         phosphor-glow drop shadow (set in code-behind since Effect.Color can't data-bind to a
         Brush directly), and a segmented LED bar instead of a continuous ProgressBar. Shares
         MeterTile's Title/ValueText/SubText/Percent/AccentBrush surface so it's a near drop-in
         replacement wherever a denser, TMOG-flavored tile is wanted. -->
    <Border Style="{StaticResource CardStyle}" Margin="0" Background="{DynamicResource BgElevatedBrush}">
        <StackPanel>
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="8" Height="8" CornerRadius="4" Margin="0,0,7,0"
                            Background="{Binding AccentBrush, ElementName=Root}" />
                    <TextBlock Text="{Binding Title, ElementName=Root}" Style="{StaticResource CardTitle}" Margin="0" />
                </StackPanel>
            </Grid>

            <!-- Inset bezel around the readout -->
            <Border Background="{DynamicResource BgBrush}" BorderBrush="{DynamicResource BorderSubtleBrush}"
                    BorderThickness="1" CornerRadius="5" Padding="10,6" Margin="0,0,0,8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="ValueTextBlock" Text="{Binding ValueText, ElementName=Root}"
                               FontFamily="Consolas" FontSize="20" FontWeight="Bold"
                               Foreground="{Binding AccentBrush, ElementName=Root}" VerticalAlignment="Bottom" />
                    <TextBlock Text="{Binding Unit, ElementName=Root}" FontFamily="Consolas" FontSize="11"
                               Foreground="{Binding AccentBrush, ElementName=Root}" Opacity="0.7"
                               Margin="4,0,0,3" VerticalAlignment="Bottom" />
                </StackPanel>
            </Border>

            <!-- Segmented LED bar -->
            <Border Height="8" CornerRadius="2" Background="{DynamicResource BgBrush}" ClipToBounds="True">
                <Border CornerRadius="2">
                    <Border.Background>
                        <MultiBinding Converter="{StaticResource SegmentedMeterBrushConverter}">
                            <Binding Path="Percent" ElementName="Root" />
                            <Binding Path="AccentBrush" ElementName="Root" />
                        </MultiBinding>
                    </Border.Background>
                </Border>
            </Border>

            <TextBlock Text="{Binding SubText, ElementName=Root}" Style="{StaticResource SubStat}" Margin="0,8,0,0" />
        </StackPanel>
    </Border>
</UserControl>
